


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Wishlist</title>
    <style>
        :root {
            --navy: #1e2d52;
            --light-gray: #f5f6f7;
            --text-gray: #666;
            --border-gray: #e0e0e0;
            --primary-blue: #4169e1;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: -apple-system, BlinkMacSystemFont, sans-serif;
        }

        body {
            background: var(--navy);
            min-height: 100vh;
            padding: 20px;
        }

        .page-header {
            color: white;
            margin-bottom: 20px;
        }

        .breadcrumb {
            color: rgba(255, 255, 255, 0.7);
            font-size: 14px;
            margin-top: 5px;
        }

        .breadcrumb a {
            color: rgba(255, 255, 255, 0.7);
            text-decoration: none;
        }

        .container {
            display: flex;
            gap: 20px;
        }

        .sidebar {
            background: white;
            border-radius: 8px;
            padding: 24px;
            width: 280px;
            height: fit-content;
        }

        .profile {
            text-align: center;
            margin-bottom: 30px;
        }

        .profile-img {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            margin-bottom: 15px;
        }

        .profile-name {
            font-weight: 600;
            margin-bottom: 5px;
        }

        .profile-email {
            color: var(--text-gray);
            font-size: 14px;
        }

        .menu-section {
            margin-bottom: 25px;
        }

        .menu-title {
            text-transform: uppercase;
            font-size: 12px;
            font-weight: 600;
            color: var(--text-gray);
            margin-bottom: 15px;
        }

        .menu-list {
            list-style: none;
        }

        .menu-item {
            display: flex;
            align-items: center;
            padding: 8px 0;
            color: var(--text-gray);
            text-decoration: none;
            font-size: 14px;
        }

        .menu-item.active {
            color: var(--primary-blue);
        }

        .menu-item span {
            margin-left: 10px;
        }

        .badge {
            background: var(--primary-blue);
            color: white;
            padding: 2px 6px;
            border-radius: 10px;
            font-size: 12px;
            margin-left: auto;
        }

        .main-content {
            background: white;
            border-radius: 8px;
            padding: 24px;
            flex-grow: 1;
        }

        .content-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        .content-title {
            font-size: 16px;
            font-weight: 600;
        }

        .items-count {
            color: var(--text-gray);
            font-size: 14px;
        }

        .wishlist-item {
            display: flex;
            align-items: center;
            padding: 20px 0;
            border-bottom: 1px solid var(--border-gray);
        }

        .item-image {
            width: 100px;
            height: 100px;
            object-fit: cover;
            margin-right: 20px;
        }

        .item-details h3 {
            font-size: 16px;
            margin-bottom: 8px;
        }

        .item-details p {
            color: var(--text-gray);
            font-size: 14px;
            margin-bottom: 4px;
        }

        .item-actions {
            margin-left: auto;
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .quantity-select {
            padding: 5px;
            border: 1px solid var(--border-gray);
            border-radius: 4px;
            width: 70px;
        }

        .price {
            font-weight: 600;
            font-size: 16px;
        }

        .action-btn {
            color: var(--text-gray);
            text-decoration: none;
            font-size: 14px;
        }

        .continue-shopping {
            display: block;
            text-align: center;
            color: var(--primary-blue);
            text-decoration: none;
            margin-top: 20px;
        }

        .logout-btn {
            position: fixed;
            top: 20px;
            right: 20px;
            background: rgba(255, 255, 255, 0.1);
            color: white;
            border: none;
            padding: 8px 16px;
            border-radius: 4px;
            cursor: pointer;
        }

        .edit-link {
            color: var(--primary-blue);
            text-decoration: none;
            font-size: 14px;
            margin-left: 5px;
        }
    </style>
</head>
<body>
    <button class="logout-btn">Log out</button>
    
    <div class="page-header">
        <h1>Wishlist</h1>
        <div class="breadcrumb">
            <a href="#">Account</a> / Wishlist
        </div>
    </div>

    <div class="container">
        <aside class="sidebar">
            <div class="profile">
                <img src="/api/placeholder/120/120" alt="Profile" class="profile-img">
                <h2 class="profile-name"><%=user.name%></h2>
                <p class="profile-email"><%=user.email%></p>
            </div>

            <div class="menu-section">
                <h3 class="menu-title">Account</h3>
                <ul class="menu-list">
                    <li><a href="#" class="menu-item">Personal info</a></li>
                    <li><a href="#" class="menu-item">Login & security</a></li>
                    <li><a href="#" class="menu-item">Notifications <span class="badge">1</span></a></li>
                    <li><a href="#" class="menu-item">Preferences</a></li>
                </ul>
            </div>

            <div class="menu-section">
                <h3 class="menu-title">Shopping</h3>
                <ul class="menu-list">
                    <li><a href="#" class="menu-item">Your orders</a></li>
                    <li><a href="#" class="menu-item active">Wishlist <span class="badge">2</span></a></li>
                </ul>
            </div>

            <div class="menu-section">
                <h3 class="menu-title">Billing</h3>
                <ul class="menu-list">
                    <li><a href="#" class="menu-item">Plans & payment</a></li>
                    <li><a href="#" class="menu-item">Address</a></li>
                    <li><a href="#" class="menu-item">Teams <span class="badge">+2 new users</span></a></li>
                </ul>
            </div>
        </aside>